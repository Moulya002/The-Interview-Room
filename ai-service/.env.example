# Optional — if omitted, the service uses deterministic heuristic generation.
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4o-mini"

# CORS (comma-separated). Defaults to "*".
ALLOWED_ORIGINS="http://localhost:3000"

PORT="8000"
